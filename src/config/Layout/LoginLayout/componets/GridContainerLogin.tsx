import { Grid, styled } from '@mui/material';

const GridContainerLogin = styled(Grid)({
  height: '100vh',
  justifyContent: 'center',
  alignItems: 'center',
  background: 'url("/images/background.jpg") center/ cover no-repeat'
});

export default GridContainerLogin;
